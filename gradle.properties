org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true
android.overridePathCheck=true
# JDK for the build: set JAVA_HOME to a JDK 21 (e.g. Android Studio's bundled JBR), or
# set the Gradle JDK in Android Studio. The machine-specific org.gradle.java.home pin was
# removed so the project builds on any OS / install location.
